import { Typography } from '@mui/material';
import Layout from '../components/@templates/Layout';

const HitPage: React.FC = () => {
  return (
    <Layout>
      <Typography variant="h1" align="center" gutterBottom>
        Welcome to My Appk hits
      </Typography>
    </Layout>
  );
};

export default HitPage;
